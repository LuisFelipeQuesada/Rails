<h1>Listing users</h1>

<table>
  <tr>
    <th>Nombre</th>
    <th>Correo electrónico</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @users.each do |user| %>
  <tr>
    <td><%= user.name %></td>
    <td><%= user.email %></td>
    <td><%= link_to 'Show', user %></td>
    <td><%= link_to 'Edit', edit_user_path(user) %></td>
    <td><%= link_to 'Destroy', user, method: :delete, data: { confirm: '¿Está seguro que desea eliminar este usuario?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'Resgistrarse en el sistema', new_user_path %>
